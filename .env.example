# Django
DEBUG=1
SECRET_KEY=change-me
ALLOWED_HOSTS=127.0.0.1,localhost

# Database (SQLite default for local)
DATABASE_URL=sqlite:///db.sqlite3

# For Railway Postgres, set DATABASE_URL from their secrets
